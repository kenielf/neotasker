package com.neotasker.model;

/**
 * Defines the aliasing types available for the system.
 */
public enum Aliasing {
    /** Synchronizes with whatever the system has set. */
    SYSTEM,
    /** Forcefully enables antialiasing. */
    ENABLED,
    /** Forcefully disables antialiasing. */
    DISABLED
}
